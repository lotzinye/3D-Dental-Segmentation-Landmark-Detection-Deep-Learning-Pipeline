# ============================================================
# dental_landmark_pipeline — Python dependencies
# Environment: Python 3.10.11 | PyTorch 2.10.0+cu126 | CUDA 12.6
# ============================================================
#
# NOTE: PyTorch is already installed — do NOT add it here.
#       Run  build_extensions.bat  AFTER pip install -r requirements.txt
#       to compile the two CUDA extensions and install torch-scatter.
#
# Install with:  pip install -r requirements.txt
# ============================================================

# --- Numerics / ML fundamentals ---
numpy<2.0.0
scikit-learn>=1.3.0

# --- 3D mesh / point cloud I/O ---
open3d>=0.17.0
trimesh>=3.22.0
pymeshlab>=2022.2

# --- Deep learning utilities ---
pytorch-lightning>=2.0.0
torchtyping>=0.1.4
timm>=1.0.0

# --- Vision transformers & sparse ops ---
# torch-scatter is installed separately via build_extensions.bat
# because it requires matching the exact PyTorch+CUDA build string.

# --- Visualisation / logging ---
wandb

# --- Misc ---
lxml>=5.0.0
opencv-python>=4.10.0
